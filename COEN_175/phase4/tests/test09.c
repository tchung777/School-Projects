char a, b;
int i, j;
int x, y;
char *s, *t;

int foo(int x)
{
    return x + y < a * b - 20;
}

int bar(void)
{
    x = 10;
    return x + b + foo(a);
}

int main(void)
{
    int *p;
    x + main;			/* invalid operands to binary + */
    x == a;
    x != b;
    '1' == j;
    x = (&i - &j);
    s == &a;
    s != &x;			/* invalid operands to binary != */
    bar() + x + p + main() + foo(x);
    s = "hello world";
}
